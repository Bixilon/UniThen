mutation(
    $appointment: ID!
    $user: ID!
) {
    appointment_checkin(
        actualAppointmentID: $appointment
        userID: $user
        expectedAppointmentID: $appointment
    ) {
        id
        message
        status

        user {
            id
            first_name
            last_name
        }
    }
}
