mutation ($attempt: ID!) {
    delete_checkin_attempt(checkinAttemptID: $attempt) {
        id
        status
        message

        user {
            id
            first_name
            last_name
        }
    }
}
