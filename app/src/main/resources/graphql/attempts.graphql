query ($appointment: ID!) {
    appointments_pk(id: $appointment) {
        id

        checkInAttempts {
            id
            status
            user {
                id
                first_name
                last_name
            }
        }
    }
}
