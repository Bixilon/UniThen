query List_Postings($userID: ID!) {
    user_pk(userID: $userID) {
        id
        postings {
            id
            product {
                id
                resource {
                    __typename
                    ... on Course {
                        id
                    }
                }
            }
        }
    }
}
