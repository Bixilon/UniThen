query ($user: ID!) {
    user_pk(userID: $user) {
        id
        postings {
            id
            product {
                id
                resource {
                    __typename
                    ... on Course {
                        id
                    }
                }
            }
        }
    }
}
